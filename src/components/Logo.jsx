import Link from "next/link";

const Logo = () => {
  return (
    <div>
      <Link href="/" className="text-3xl font-extrabold tracking-wide">
        SHOP.CO
      </Link>
    </div>
  );
};

export default Logo;
